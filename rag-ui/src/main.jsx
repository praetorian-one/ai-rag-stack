import React from 'react';
import ReactDOM from 'react-dom/client';
import RagUI from './App';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <RagUI />
  </React.StrictMode>
);
